services:
  - type: web
    name: procdcour
    runtime: python
    repo: https://github.com/juriprudence/procdcour.git
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers=2 --bind=0.0.0.0:$PORT